# Gate 1 run 001

Purpose: execute the frozen 20-household gallery-ingestion audit without target-object coding.

This run validates whether the EstateSales.NET legacy sale-data endpoint returns usable ordered `sale.pictures[]` arrays across the 2010–2025 calibration frame.
